name: Keyword Pipeline

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * 1'  # Every Monday at 6 AM UTC

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Step 1 - Expand seeds
        env:
          GOOGLE_ADS_DEVELOPER_TOKEN: ${{ secrets.GOOGLE_ADS_DEVELOPER_TOKEN }}
          GOOGLE_ADS_CLIENT_ID: ${{ secrets.GOOGLE_ADS_CLIENT_ID }}
          GOOGLE_ADS_CLIENT_SECRET: ${{ secrets.GOOGLE_ADS_CLIENT_SECRET }}
          GOOGLE_ADS_REFRESH_TOKEN: ${{ secrets.GOOGLE_ADS_REFRESH_TOKEN }}
          GOOGLE_ADS_CUSTOMER_ID: ${{ secrets.GOOGLE_ADS_CUSTOMER_ID }}
          DATAFORSEO_LOGIN: ${{ secrets.DATAFORSEO_LOGIN }}
          DATAFORSEO_PASSWORD: ${{ secrets.DATAFORSEO_PASSWORD }}
        run: python scripts/1_expand_seeds.py

      - name: Save after Step 1
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f raw/*.csv 2>/dev/null || true
          git diff --cached --quiet || git commit -m "checkpoint: Step 1 expanded.csv [$(date +%Y-%m-%d)]"
          git pull --rebase
          git push

      # - name: Step 2 - Competitor keywords
      #   env:
      #     DATAFORSEO_LOGIN: ${{ secrets.DATAFORSEO_LOGIN }}
      #     DATAFORSEO_PASSWORD: ${{ secrets.DATAFORSEO_PASSWORD }}
      #   run: python scripts/2_competitor_keywords.py

      - name: Step 3 - Merge and dedupe
        run: python scripts/3_merge_dedupe.py

      - name: Save after Step 3
        if: always()
        run: |
          git add -f raw/*.csv 2>/dev/null || true
          git diff --cached --quiet || git commit -m "checkpoint: Step 3 merged.csv [$(date +%Y-%m-%d)]"
          git pull --rebase
          git push

      - name: Step 4 - Enrich data
        env:
          GOOGLE_ADS_DEVELOPER_TOKEN: ${{ secrets.GOOGLE_ADS_DEVELOPER_TOKEN }}
          GOOGLE_ADS_CLIENT_ID: ${{ secrets.GOOGLE_ADS_CLIENT_ID }}
          GOOGLE_ADS_CLIENT_SECRET: ${{ secrets.GOOGLE_ADS_CLIENT_SECRET }}
          GOOGLE_ADS_REFRESH_TOKEN: ${{ secrets.GOOGLE_ADS_REFRESH_TOKEN }}
          GOOGLE_ADS_CUSTOMER_ID: ${{ secrets.GOOGLE_ADS_CUSTOMER_ID }}
          DATAFORSEO_LOGIN: ${{ secrets.DATAFORSEO_LOGIN }}
          DATAFORSEO_PASSWORD: ${{ secrets.DATAFORSEO_PASSWORD }}
        run: python scripts/4_enrich_data.py

      - name: Save after Step 4
        if: always()
        run: |
          git add -f raw/*.csv 2>/dev/null || true
          git diff --cached --quiet || git commit -m "checkpoint: Step 4 enriched.csv [$(date +%Y-%m-%d)]"
          git pull --rebase
          git push

      - name: Step 5 - Classify intent and merge
        run: python scripts/5_classify_intent.py

      - name: Commit results
        if: always()
        run: |
          git add output/keywords_final.csv raw/*.csv 2>/dev/null || true
          git diff --cached --quiet || git commit -m "Update keywords_final.csv [$(date +%Y-%m-%d)]"
          git pull --rebase
          git push
